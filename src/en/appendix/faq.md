# Appendix: FAQ

## Q: Where should I put images?

Common approaches:

- Put them under `src/<lang>/assets/` (create it if needed, e.g. `src/en/assets/`)
- Reference them with relative paths, e.g. `![logo](assets/logo.png)` or `![logo](../assets/logo.png)`
